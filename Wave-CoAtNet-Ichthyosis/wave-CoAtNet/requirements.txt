# WaveCoAtNet: Ichthyosis Classification
# Requirements file — all packages needed to reproduce experiments

# ── Core Deep Learning ────────────────────────────────────────────────
torch>=2.1.0
torchvision>=0.16.0

# ── Model Architectures ───────────────────────────────────────────────
timm>=0.9.12        # ConvNeXt-Tiny, ViT-B/16, Swin-T pretrained weights
torchinfo>=1.8.0    # model summary / parameter count

# ── Scientific Computing & Visualization ──────────────────────────────
numpy>=1.24.0
scipy>=1.11.0       # McNemar's test (scipy.stats)
matplotlib>=3.7.0
seaborn>=0.12.0
tqdm>=4.65.0

# ── Machine Learning Metrics ──────────────────────────────────────────
scikit-learn>=1.3.0  # StratifiedKFold, classification_report, confusion_matrix

# ── Dataset Management ────────────────────────────────────────────────
roboflow>=1.1.0

# ── Image Processing (Grad-CAM overlay) ──────────────────────────────
opencv-python>=4.8.0

# ── Foundation Models (2026 Q1 required baselines) ───────────────────────────
open_clip_torch>=2.24.0    # BiomedCLIP baseline
transformers>=4.40.0        # DINOv2 baseline (facebook/dinov2-base)

# ── Standard Utilities ────────────────────────────────────────────────────────
pillow>=9.5.0
pandas>=2.0.0
